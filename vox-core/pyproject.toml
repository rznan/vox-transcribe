[project]
name = "vox-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "grpcio>=1.83.0",
    "protobuf>=7.35.1",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.51",
]

[dependency-groups]
dev = [
    "aiosqlite>=0.22.1",
    "grpcio-tools>=1.83.0",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.15.1",
]
